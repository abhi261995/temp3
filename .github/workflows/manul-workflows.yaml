#Manual workflow
name: manual run
on:
   push:
    branches:
     - 'new_branch/**'
jobs:
 on-demand: 
  runs-on: ubuntu-latest
  steps:
   - name: manual trigger
     run: echo "This is manaual trigger"
      